function TeamIndividualColorThemeObj(primaryColor, baseColor, shadowColor) {
  this.primaryColor = primaryColor;
  this.baseColor = baseColor;
  this.shadowColor = shadowColor;
}

export default TeamIndividualColorThemeObj;
